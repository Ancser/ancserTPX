# 1.0.9 FIX LIST(對照 docs/1.0.9_SKILL_REPORT.md 路線圖)

規則同 1.0.8:in-file `1.0.9:` 標記、每項留檔、compileall + pytest 後才算完成、核心行為改動先確認。
前提:**P0 未通過(影子重放連續 ~2 週 day_pass)前,不上新策略真錢、不信任何回測勝利。**

### P0|影子重放(實盤平價層)— 完成,煙測抓到現行犯

- `backend/backtest/shadow_replay.py`(新模組):每交易日把當日實盤成交(data/trades.json,跨帳號去重)按 `param_snapshot_id` 分組,用**同一份參數快照**(data/strategy_snapshots.jsonl,1.0.8 建)重跑回測,貪婪配對(方向同、時間 ±10min、價 ±8 tick)→ 吻合率、live-only、bt-only、滑價/時差;日報持久化 `data/shadow_replay/YYYY-MM-DD.json`。通過線 match_rate ≥ 0.90。
- **重放窗口 = 目標日 + 前 5 個交易日**(zone 參考只需 10 個 bucket;更貼近 live 2 天 warmup;多快照日從 5+ 分鐘降到 ~40 秒)。因窗口裁剪與 timeline 索引不相容,重放走 detector 慢路徑(小窗口夠快),不注入快取 timeline。
- routes:`POST /api/live/shadow-replay`(手動,可帶 trade_date)、`GET /api/live/shadow-replay/recent`(近 n 份日報 + `consecutive_pass_days` + `p0_cleared`≥10 日);`shadow_replay_daily_task()` 每日 **20:10 UTC**(flatten 後)自動跑並以 WARN 級寫 server log(PASS ✅ / FAIL ❌ + 各快照摘要)。
- `backend/main.py` lifespan:啟動掛每日任務、關閉 cancel。
- **首跑實測(2026-07-03)**:snapshot 302dfa5b(trend/tp)live=15 / bt=5 / matched=4 → rate 0.267 **FAIL**;另一 snapshot 1/1 PASS。執行錯配從「感覺」變成每日量化指標,方向:live 過度進單(vs 6 月研究的進單不足)→ 待逐筆看 live-only 明細定位(重啟殘留 state?多帳號未同步 lock?)。

### P1|驗證紀律(制度性反過擬合)— 完成

- `backend/backtest/sweep.py`:每變體新增 `seg_pnls`(日期跨度三等分 walk-forward 各段 pnl)、`wf_pass`(三段各正);`_annotate_plateau_and_acceptance()` — **平原測試**(單維 ±1 檔鄰居 ≥60% 為正且自身為正)+ **接受標準** `accept = wf_pass ∧ plateau_pass ∧ trades≥80 ∧ expect>0`。
- 前端 SWEEP 表新增 **WF**(✓/✗,tooltip 顯示三段 pnl)與 **ACC**(★,整列淡綠底)欄。
- 紀律入 SKILL 報告 E 節:不再同窗掃同窗報;7 月起資料視為封存段。

### P2|指標過濾工廠 — 腳本完成,結果待背景任務

- `scripts/indicator_filter_study.py`:RSI14 順向/不過熱、EMA20/50 同向、KDJ(%K/%D)同向、BB z>2.2 阻擋、ATR z∈[-1,1] 中波動 — 7 個門 × FABLE#2(VA70 ladder C3 S0)基準,timeline 快路徑,輸出含 walk-forward 三段與 ACC 判定。先驗:1.0.8 量價過濾全滅,本輪測完歸檔。

### P3|Per-candle 特徵庫 + baseline ML — 腳本完成,結果待背景任務

- `scripts/build_candle_features.py` → `data/candle_features.csv`(~7 萬行 × 24 欄):多尺度報酬、RV30、效率比60、量 z、實體/上下影、delta 代理60、ATR14、距前日 VAH/VAL/POC(ATR 單位)、session/時鐘(sin/cos);標籤 `y_up1r`(未來 60m ±1ATR triple-barrier-lite,同根雙碰保守記 0)+ fwd15/fwd60。
- `scripts/ml_baseline.py`:numpy logistic(L2)+ 按日期三段 walk-forward,只報 OOS AUC + top 權重;**< 0.55 = 依紀律宣佈 1m 表面數據無訊號**。

### 1.0.9 批次B:fade 收斂 + 兩模式 + prevRV option + UI 重排 + shadow PnL 背離 — 完成,pytest 23 passed

- **FADE 收斂為單一最佳版**:`backend/strategy/fade.py` PrevDayFade 加 `TP_FRAC`(TP=VAL+frac·(POC-VAL))與 `ENTRY_MODE`(limit|rejection);最佳 = **SL120 / TP0.75 / limit**(引擎實測 31 筆 +1307 / DD305 / PF2.13,walk-forward 三段皆正)。舊 SL80/TP=POC 證過擬合(多 1 日 +1255→+86)。兩 registry 的 fade #4/#5 併為單一 `07.03 FADE …`;舊名經 PRESET_RENAMES 自動遷移。**rejection 版**:掃破 VAL 又收回 → 市價追(勝率高但 walk-forward 第 2 段翻負,非預設,可選)。
- **prevRV regime gate 成為 risk-management option**:`StrategyParams.tr_prev_rv_gate`(回看天數,0=OFF);backtest engine 日 rollover 算前一日 RV、與近 N 日高三分位比較 → 封鎖今日新單;live engine 對稱(`_refresh_prev_rv_gate` 掛 warmup+rollover,entry gate 攔截)。實測 FABLE#2:gate15 → DD 915→494(-46%),pnl 折半(擋高波動日=擋趨勢書糧倉,屬 DD 換 PnL 的取捨)。UI risk 面板加「PREV-RV 波動閘」下拉。
- **新 StrategyParams 欄位** `tr_prev_rv_gate` / `fade_tp_frac` / `fade_entry_mode` 全鏈路貫通(models、terminal_live `_build_strategy_params`+allowed_keys、routes `_build_strategy_params_from_request`+BacktestRequest/LiveStartRequest、前端 build/apply)。
- **UI**:PRESET 選擇移到 CONTRACT 上方(bt+live);risk 面板加「FADE 進場模式」(僅 fade 顯示)與**唯讀 SL 模型顯示**(trend=最低量節點 / fade=VAL−120t / sigma=開盤±Nσ,依策略自動更新,置於 RR/TP 之上,不可改)。cache-bust `&fade109=1`。
- **Shadow replay 逐筆 PnL 背離**(回應「entries 對得上但 PnL 差」):每配對加 `live_pnl/bt_pnl/pnl_diff`,匯總 `matched_live_pnl/matched_bt_pnl/pnl_diff_total/worst_pnl_diffs`。7/3 實測:matched pairs slip 0 tick、pnl 差 ~$1(近乎一致)→ 證實錯配來自 **live-only 多開的單**(live 15 vs bt 5),非滑價;方向鎖定「live 過度進場」。

### 1.0.9 P0 BUGFIX:shadow replay 裁窗口導致回測嚴重少出單(誤判執行錯配)— 已修

- **症狀**:首版 shadow replay 為求快把回測 K 線裁成「目標日+前 5 個交易日」,zone 偵測器暖機不足 → 回測當日大幅少出單。7/3 回測顯示 5 筆,實盤 18 筆,被我誤讀為「live 多開 10 筆、過度交易」。使用者質疑:Topstep 顯示 20 筆、回測也該 ~20,哪來的多單?
- **查證**:trades.json 7/3 = **18 筆**(單帳號 22373660,全 trend,去重 0)、進場 7/2 22:05→7/3 06:20 UTC(ASIA 盤,交易日邊界 CT17:00 正確歸到 7/3)。用**完整歷史**跑同 snapshot(302dfa5b:trend / exit=**tp** / stop=**0** / VA70 RR4 C3)→ 回測 7/3 = **20 筆**,形態一致(13 個 -41 SL + 2 個 +159 TP)。逐筆進場時刻對得上。
- **結論修正**:**執行沒有錯配,live≈backtest**。7/3 是震盪虧損日,回測同樣虧 ~-180(策略 64.8% 假突破的真面目)。真正問題:當日 live 跑的是**裸 tp、無斷路器**配置 → 13 連環 -1R 無攔截。開日虧斷路器(S4)可把當日封頂在 ~4 單、ladder 可讓 2 個 +159 winner 跑更遠。
- **修法**:`shadow_replay.run_shadow_replay` 移除窗口裁剪,改用完整歷史;routes `_shadow_timeline_provider` 對單 TF 注入快取 zone timeline 保速度(overlap/session 走 detector)。match_rate 由 0.55(bug)→ ~0.90(正確)。
- **教訓**:P0 是量測執行偏差的尺 — 尺本身不能有偏差。任何為速度而近似的做法(裁窗口)都可能製造假的「錯配」訊號。正確性 > 速度。

### 1.0.9 P0 FIX #2:shadow replay 改讀 broker 真相 + 鎖主帳號 — 完成,對得上 Topstep

- **根因**:`trades.json`(加料版)的 exit_price/pnl 有 bug(7/3 一筆記 exit=30135、pnl=-12.5,broker 真相是吃滿止損 -42.24)。shadow replay 讀它 → 每日對賬全錯(7/3 顯示 -57.5,真實 -183.8)。
- **修法**:`_load_live_fills` 改讀 `data/trade_history.json`(broker 原始成交,含 gross/net/手續費);`main_account_id()` 自動偵測主帳號(記錄最多者)= 22373660,**只比主帳號**(其餘 4 個是跟單 follower,忽略);`_day_snapshot_id` 從 trades.json 只取 snapshot tag(還原參數用,不碰其 pnl);live engine 啟動時 `set_main_account(account_id)` 覆寫。
- **驗證**:主帳號 7/3 broker net = **-183.8 = Topstep -183.4**(完全對上);shadow 實盤 20 筆 = 回測 20 筆(**數量一致,確認無過度交易 — 先前「多開 10 筆」全錯,徹底作廢**)。
- **執行差異的真正原因(實測,推翻「暖機說」)**:實測 2 天暖機 vs 75 天完整歷史 → 回測出**一模一樣的 20 筆單、同價格**。故暖機窗口與 zone 演算法**都不是**原因(5m 桶 VP 是本地計算,與歷史長度無關)。逐筆對齊 20 筆:**10 筆進場價精準一致、7 筆 live 成交更好、2 筆略差、1 筆 live 多出**。差異來自兩個良性來源:(a)**成交價改善** — 回測假設 limit 剛好在 zone 邊界成交,live 的 limit 常在回撤時拿到更好價;(b)**1 分鐘時序差** — live 用「已完成的上一根 K」決策(安全緩衝),偶爾選到略不同的參考桶 → 3 筆進場點差較大。**不是 slip、不是入場錯、不是過度交易、不是 hunt。**
- 配對器改為判「同決策」(±12min + 同方向,價格放到 120tick 寬鬆上限只擋明顯不同單,價差記入 price_slip_ticks 供分析)→ 7/3 match_rate **0.85**(17/20 同決策同價,進場價差中位 **0 tick**),實盤配對 PnL 優於回測。
- **FADE 更穩,不需特別暖機同步**:fade 用前一整天 VP(~1380 根),大而穩定,遠比 5m 桶不敏感;暖機連 5m 都不影響,fade 更不受影響,只要 warmup 涵蓋前一交易日(現抓 2-14 天足夠)。

### 1.0.9 批次C:目標改 PF-first + risk 面板重排 + live 風控閘狀態列 — 完成(py_compile + node --check 通過)

- **目標(SKILL.md)由 maxDD-first 改為 PF-first**(使用者 2026-07-05):排序/接受不再以單一 maxDD 為主鍵,改以 **profit factor 與 PF 穩定度**(walk-forward 三段 PF 各 >1)為第一準則,理由是「maxDD 只是一個被幸運趨勢月壓低的最壞點;PF 一漂移,高 total loss 就不穩」。`maxDD < $2,000` 降為 **硬性 Topstep 帳戶閘**(非排序鍵),過 PF 但破 2k = research-only。接受標準寫死:PF≥~2.0、每段 PF>1、每筆期望>0(含成本)、樣本≥80、平原(±1 檔仍正)、total loss<PnL(佳<70%)。改動 `SKILL.md` Major goal + 步驟 5。
- **RISK MANAGEMENT 面板重排**(bt+live,`ancserTPX.html`):執行/出場參數群組在上且相鄰 —— **EXIT MODE → SL 模型 → RR RATIO → TRAIL TP TRIGGER → TRAIL SL**;封鎖型 gate 移到其後 —— **日虧斷路器 → PREV-RV 波動閘 → FADE 進場模式**。純 DOM 重排,ID 不變(逐一 grep 確認 bt/live 各出現 1 次,無重複/遺漏)。
- **Live 風控閘即時狀態列**(回應「監控狀態列顯示每個封鎖限制現在生不生效」):
  - 後端 `backend/live/engine.py::get_status()` 新增 `risk_gates` 區塊 —— `daily_loss{limit,count,resting}`、`prev_rv{lookback,blocking}`、`session_limit{on}`、`tp_lock{on,locked}`。純唯讀遙測,**不改任何進單/出場行為**。
  - 前端 `ancserTPX.js` 新增 `renderLiveRiskGates(st)`,在 `pollLiveStatus` 每次輪詢(running/stopped 皆)更新;top-bar 新增 `#lv-riskgate-row`(盤段/日限/日虧/波動閘/TP鎖 chips,綠=通行 灰=OFF 紅=封鎖休息 琥珀=已計數未達),LIVE STATUS 側欄鏡像「日虧斷路 / 波動閘」兩行。盤段是否在允許窗前端用 `active_allowed_sessions` vs `getMarketSession()` 判定。cache-bust `&riskgate109=1`。
  - **注意**:因 `get_status()` 屬後端,需照 no-`--reload` 規則**整個關掉重開**(close cmd + 重跑 .bat)風控閘數字才會動;純前端(chips/重排)刷新頁面即生效。

### 1.0.9 批次D:OR15 開盤假突破 fade 移植進生產引擎(practice-live ready)— 完成(pytest 23 passed + 單元煙測)

- **來源**:`scripts/fade_professional_idea_sweep.py` 的 `or15_false_break` 最佳生產可用變體(報告 `docs/1.0.9_FADE_PROFESSIONAL_IDEA_SWEEP_REPORT.md`:ALL / SL=0.2Rng / full_1r / maxE=1 → 70 筆 +7178 / DD930 / **PF2.55** / win34%)。pyramid 版(PF3.05/win82%)未移植 —— 需部分出場,非引擎的全倉 TP 行為。
- **實作**:`backend/strategy/fade.py` 新增 **`OpeningRangeFade`** 類(與 PrevDayFade/SessionTrendFollow 介面相容)。規則:OR 視窗 = RTH 開盤 13:30–13:45 UTC 逐根更新高/低;t≥13:45 後,高假突破(high>ORH 且 close<ORH)→ 做空、低假突破(low<ORL 且 close>ORL)→ 做多;`rng=前日 VAH−VAL`(由既有 fade set_levels 餵入),SL 距 `max(4t,0.2×rng)`、TP 距 `max(4t,1.0×rng)`、market@close、每交易日每方向一次;OR 於 set_levels 換日重置。
- **接線(最小侵入)**:兩引擎 `strategy_mode=="fade"` 時依 `fade_entry_mode` 選類 —— `"or15"`→OpeningRangeFade,其餘→PrevDayFade(`backend/live/engine.py`、`backend/backtest/engine.py` 各 1 處 + import)。**strategy_mode 仍是 "fade"**,故前日 VP 水位餵入、zone 渲染、eval_zones 等所有 fade 管線原封不動。
- **參數放行**:`fade_entry_mode` 白名單加 `"or15"`(`routes.py`、`terminal_live.py` 的強制轉換;前端 `collectStrategyParams`/`applyPreset`)。前端 fade 進場模式下拉加「OR15 開盤假突破(雙向·市價)」選項 + onchange 刷新唯讀 SL 模型顯示(fade 子模式感知)。
- **Preset**:`07.05 CLAUDE #1 FADE OR15 LIVE PRACTICE MNQx1 SL0.2R TP1R Market ALL maxE1` —— **MARKET SESSION = ALL(必須含 RTH,否則盤段過濾擋掉 13:30–13:45 → OR 無法累積)**、雙向(session-limit OFF)、exit=tp、trail OFF、日虧斷路器 2、fade_entry_mode=or15、practice account 可直接測下單。
- **煙測(單元)**:前日 rng=50 → 假突破上 → SELL、SL=+10(=0.2×50)、TP=−50(=1×50)、market;同日同向再觸發被鎖、反向仍可;OR 視窗內不進場。全部符合預期。
- **紀律狀態**:**practice-live/backtest ready** —— practice account 可直接測 fake-money 下單;express 仍需使用者手動選帳號與 preset。下一步:在生產引擎跑回測對照研究腳本的 +7178/DD930/PF2.55,再用 practice fills 檢查 live/backtest 差異。

### 1.0.9 批次E:多帳號 Account 頁 + leader/follower 分組 + 每 leader 一 preset + 2 引擎並行 + 多帳號交易紀錄 — 完成(pytest 23 + 單元煙測;live 並行需使用者實機驗證)

- **需求(使用者 2026-07-05)**:1 practice + 1 express = 2 個 leader,各跑自己 preset(可一多一空,對衝允許);4 個 exam/combine 為 follower(app 外部複製跟單),偵測後巢狀列在 leader 底下;交易紀錄要能容納多帳號、歸在主 email 下。**「一次全做」。**
- **後端角色層**:`backend/live/account_roles.py`(新)—— 持久化 `data/account_roles.json`(email / leaders{accId:{preset}} 最多 2 / followers{accId:leaderId});`save_roles` 驗證(≤2 leader、follower 需指向有效 leader、leader 不可兼 follower);`annotate_accounts` 套角色 + 名稱建議 + 把 follower 巢狀到 leader。routes:`/accounts` 回應加 role/preset/followers 分組;新 `GET/POST /accounts/roles`。
- **後端 2 引擎 registry(核心改動,additive)**:`_live_engine` 單例 → `_live_engines: Dict[int, engine]`(最多 MAX_LIVE_ENGINES=2);`_live_engine` 永遠對齊 primary(既有 ~15 處單引擎引用零改動);`_primary_live_engine`/`_resolve_live_engine`/`_sync_primary_engine` helper。`/live/start` 改為同帳號不可重啟、最多 2 併行、註冊進表;`/live/stop|flatten|cancel-pending|status` 加 `account_id` 參數(0=primary);新 `GET /live/status-all`(Account 頁多帳號監控)。
- **後端交易紀錄多帳號**:`_annotate_trade_accounts`(標 role + account_name 快取)、`_trade_history_by_account`(分帳號筆數/淨 PnL,leader 優先)、`_trade_history_response`(統一回應含 `by_account` + `email`)。`/live/trade-history` 預設過濾仍鎖「主帳號」(primary 引擎 → 否則 roles 第一 leader → 否則全部),**保持 calendar 單帳號檢視不被 follower 複製單灌水**;多帳號總覽走 `by_account`。refresh 時填 `_account_name_cache`。
- **前端 Account 頁**:header 加 `data-tab="account"` 分頁 + `#account-view` 全頁 overlay(仿 calendar);tab 切換掛載/卸載 `pollAccountStatus`。每帳號卡:角色下拉(none/leader/follower)、leader→preset 下拉 + GO LIVE/STOP/FLAT、follower→leader 下拉;follower 巢狀縮排在 leader 下。`SAVE ROLES` 存檔;`accountGoLive` 用該 leader 指定的 preset 物件組 `/live/start` body(**confirm 對話框**,FUNDED 帳號紅字警告;app 絕不自動下單);`pollAccountStatus` 走 `/live/status-all` 只更新各卡狀態 span 不重繪(不干擾編輯中的下拉)。
- **驗證**:py_compile + node --check 通過;pytest 23 passed;account_roles 單元(≤2 leader、無效 follower 丟棄、leader 不兼 follower、巢狀正確);trade-history 分組單元(filter、by_account role/name、leader 優先排序)全過。
- **未驗證/使用者實機**:**2 個 leader 引擎同時對真實帳號並行下單**這條 live 路徑我無法在此整合測試 —— 依專案紀律當 needs-verification:先單一 leader GO LIVE 確認、再啟第二個。`set_main_account`(shadow replay 主帳號)在 2 leader 下會被後啟者覆寫,屬 shadow 分析範疇、非交易安全問題,待觀察。
- **cache-bust** `&acct109=1`。後端改動需整個關掉重開才生效。

### 1.0.9 批次E-2:Account 頁依實機簡化 —— 3 類型 + 每帳號 preset + 2 live + 固定 main(去除 leader/follower)— 完成(pytest 23 + 單元)

- **依使用者 Topstep 實機畫面修正**:帳號**類型由名稱三分** —— 含 `EXPRESS`→**express**(Express Funded,真錢)、含 `PRAC`→**practice**、其餘(50KTC…Evaluation)→**exam**(評估,模擬)。取代舊 `is_practice`→PRACTICE/FUNDED 二分。`simulated = account_type != "express"`。
- **模型簡化(去除 leader/follower)**:`account_roles.json` 改為 `{email, main_account_id, accounts:{id:{preset, live}}}`。`save_roles` 驗證:**最多 2 個 live**(超出降級)、`main_account_id` 須為有效帳號且優先指向某個 live 帳號。新增 `classify_type()` / `main_account_id()`;`annotate_accounts` 附 `account_type/type/simulated/preset/live/is_main`。
- **routes 對齊**:`/accounts` 帶三類型;`AccountRolesRequest` 改 `main_account_id/accounts`;trade-history `_annotate_trade_accounts` 用 `account_type + is_main`(非 role),`by_account` 主帳號優先排序,預設過濾 fallback 由「第一 leader」改為 `main_account_id`。
- **前端 Account 頁重做為對齊表格**(仿 Topstep 畫面):欄位 NAME / TYPE(色碼 + EXPRESS 標 `$REAL`)/ BALANCE / STATUS / **PRESET 下拉** / **LIVE 勾選(≤2,超過自動取消)** / **MAIN 單選(需先 LIVE)** / GO·STOP·FLAT / RUNTIME。移除角色下拉與 follower 巢狀。GO 的真錢警告改看 `account_type==='express'`。排序 express→practice→exam。
- **驗證**:node --check + py_compile 通過;pytest 23;account_roles 單元(三分類、2-live 上限、main 驗證/自動指派、annotate 全欄)+ trade-history 分組單元全過;無殘留 `_acctRoleOf/_acctLeaderOf/leaders/followers` 引用。cache-bust `&acct109=2`。
- **不變**:2 引擎 registry、`/live/*` 帶 account_id、`/live/status-all`、by_account —— 沿用批次E。**2 帳號並行真下單仍 needs-verification**(先單一 LIVE 驗證再啟第二)。

### 1.0.9 批次E-3:Live Monitor 側欄改成 ACCOUNT 1 / ACCOUNT 2 兩帳號槽 — 完成(node --check + pytest 23)

- **需求**:把 live monitor 側欄的單一 ACCOUNT + LIVE STATUS 面板,改成 **ACCOUNT 1 / ACCOUNT 2** 兩槽,各自可選帳號、選 preset、GO/STOP/FLAT、看即時狀態(狀態與標題合併進各槽)。
- **HTML**:移除 `live-account-select` / `btn-go-live|stop|flatten` / `LIVE STATUS` 面板,換成兩個對稱面板;每槽 = 帳號下拉 + **preset 下拉(在帳號下方)** + GO/STOP/FLAT + 狀態塊(STATUS 燈/PHASE/MODE/MARKET/日虧/波動閘/DAILY PNL,ids `live-slot-*-{1,2}`)。
- **JS(新自足模組)**:`initLiveSlots`(填兩槽帳號 + preset;localStorage 記憶;預設 slot1=main/express、slot2=practice)、`liveSlotGoLive/Stop/Flatten(slot)`(用該槽 account+preset 打 `/live/start`|`/live/stop?account_id`|`/live/flatten?account_id`;**兩槽不可同帳號**;confirm + EXPRESS 真錢警告)、`pollLiveSlots`+`_liveSlotRenderStatus`(走 `/live/status-all` 分槽更新,含 risk_gates 的日虧/波動閘)、`_startLiveChartForAccount`(GO LIVE 後帶動既有圖表/頂欄跟隨該帳號)。
- **接線**:`loadAccounts` 末 + live 分頁切換都呼叫 `initLiveSlots`;live 分頁掛 `_liveSlotInterval`(2s)、離開清除;`pollLiveStatus` 改 `fetch('/live/status?account_id='+liveAccount.id)` 讓頂欄/圖表跟隨聚焦帳號。舊 `goLive/stopLive/flattenLive/onLiveAccountSwitch` 成無呼叫的 dead code(保留不刪,pollLiveStatus 對已移除的按鈕 id 皆有 `if(el)` 護欄)。
- **驗證**:node --check;HTML 無殘留舊 id;slot ids 各 2;新 4 函式在;pytest 23。cache-bust `&acct109=3`。**兩槽同時真下單仍 needs-verification**(先單槽驗證);圖表/頂欄跟隨「最後 GO LIVE 或聚焦」的帳號。

### 1.0.9 批次F:全策略 SWEEP + PRESETS 分頁 + 依 PF 排序 + 一鍵存結構化 preset — 完成(node --check + pytest 23 + 引擎煙測)

- **後端(使用者已完成)**:`backend/backtest/sweep.py` 擴為三策略 grid —— TREND(VA×exit×RR×confirm×stop,timeline 快路徑)+ **DAY ZONE**(limit/rejection × SL{80,120,160} × TP{.5,.75,1} × stop + or15)+ **DISTRIBUTION**(window{15,30,60}×method{std,mad}×entry{blind,reject}×accept{none,filter}×stopspan{.75,1,1.5}×target{half,center});`run_model_sweep` 統一跑三者(合併進度),`_run_one` 依 `params.strategy` 走引擎(fade/sigma timeline=None);`_annotate_plateau_and_acceptance` 泛型化(**分 model 平原 + PF-first 接受**:wf 三段 pnl>0∧pf>1 + 平原 + 樣本≥80 + 期望>0 + PF≥2 + maxDD<2000 + loss<pnl)。route `/backtest/sweep` 已改呼叫 `run_model_sweep`。約 345 變體,~10–15 分鐘。
- **前端(本輪)**:
  - EXECUTE BACKTEST 下方新增**橘色 `SWEEP ALL STRATEGIES` 按鈕**(`btn-sweep`,連線後啟用)+ `runBacktestSweep()`(confirm → 切 PRESETS 分頁 → POST `/backtest/sweep` → 進度沿用 backtest progress → `loadSweepResults` + `renderSweepTable('pf')`)。
  - 底部新增 **PRESETS 分頁(在 BACKTEST TRADES 之前)**:`btab-presets`(RUN SWEEP 按鈕 + sweep-meta + `sweep-results-wrap`);bottom-tab 切換納入 `presets`。
  - `renderSweepTable` **預設依 PF 排序**(`_sweepSortKey='pf'`),存排序後列 `_sweepRenderedRows`,每列加 **`+` 按鈕** → `saveSweepPreset(i)`:base=當前 backtest 表單、overlay=該列掃出的參數 + strategy → `buildPresetName`(結構化命名)→ `/presets/save`(**CONTRACT/SIZE 來自表單,不在掃描範圍**)。
- **煙測**:合成 4 日 1m K 跑 `_run_one` 三策略(DAY ZONE/DISTRIBUTION/TREND)皆正常出結果 + 正確 model 標籤;node --check + py_compile + pytest 23 全過。cache-bust `&sweepui109=1`。
- **注意**:sweep 是長回測(~350 變體),由使用者按鈕觸發(非我自動跑);後端 sweep.py/route 屬後端,需整個關掉重開才生效。

### 1.0.9 批次F-2:SWEEP 長期記錄 + ACC 篩選 + 移除 presets 頁按鈕 + 當機防護

- **長期記錄檔**(回應「SWEEP 後有長期記錄嗎」):route 每次 sweep 除了覆寫 `data/sweep_results.json`(最新,供分頁載入),另存**時間戳全檔** `data/sweep_runs/sweep_<UTCstamp>.json` + 追加摘要到 **`data/sweep_history.jsonl`**(created_at/variants/accepted/by_model/top);新 `GET /backtest/sweep/history?limit=N`。
- **ACC ★ 篩選**:PRESETS 分頁加「只顯示 ACC ★ 通過」勾選 → `renderSweepTable` 依 `r.accept` 過濾(過濾後 `_sweepRenderedRows`/meta/`+` index 同步;0 筆給提示)。
- **PRESETS 頁移除 RUN SWEEP 按鈕**(依 0.15.5:純列表);sweep 只從側欄橘色 `SWEEP ALL STRATEGIES` 觸發。
- **當機防護**(回應「SWEEP win11 死機/太多同時運行」):(a) `runBacktestSweep` 開跑前查 `/live/status-all`,若有 live 引擎在跑 → 警告「和 live 同時跑可能卡頓/當機,建議先 STOP live」;(b) `run_model_sweep` 三段間 + 完成後 `gc.collect()` 釋放峰值記憶體(trend 2 條 timeline);(c) route 完成後 gc。診斷:~345 個全回測、~10–15 分鐘,和 2 個 live 引擎+瀏覽器搶資源時 Win11 WebView2 易卡死 —— 根治可另做「sweep 移到獨立子行程」(待評估)。
- 驗證:node --check + py_compile + pytest 23。cache-bust `&sweepui109=2`。

### 1.0.9 批次G:FULL WIN LOCK + 使用者 ACC 條件 + sweep 新欄位 + 啟動載入修復

- **PRESETS 分頁啟動即載入**:`loadSweepResults()` 過去只在跑完 sweep 後被呼叫 → 啟動時面板空白;現於啟動尾端自動載入 `data/sweep_results.json`。
- **FULL WIN LOCK**(`tr_daily_win_stop`,0=OFF):當日贏 N 單後停新單(斷路器反向,落袋鎖利)。全鏈路:models → 兩引擎(計數/換日重置/進場 gate/live 記錄 + risk_gates.daily_win)→ routes 兩 request model → terminal allowed_keys → UI select(bt+live)+ collect/apply。
- **UI 更名**:日虧斷路器→FULL LOSS LOCK、PREV-RV 波動閘→HIGH VOLATILITY LOCK、SESSION LIMIT→SESSION MAX TRADE LIMIT、MARKET SESSION→MARKET LIMIT。
- **sweep 新指標**:long/short 分開勝率、週變異 CV(std/|mean|)、trades_per_month;`_scaled_stats` 逐筆換算 MNQx3 / NQx1(成本精確:net'=(net+1.24)×mult−cost′)。
- **使用者 ACC(2026-07-07)**:月PnL>3000 ∧ PF>1.5 ∧ 月20筆 ∧ maxDD<1000 ∧ 週CV<1(前提 wf+plateau);月PnL 100..3000 ∧ PF>2 ∧ 月>20筆 ∧ DD<500 → 縮放測試,通過則以 `contract_scale`(MNQx3/NQx1)標記接受。
- **PRESETS 表新欄位**:# / CONTRACT@x / MODEL / FACTORS / MODEL PARAMS / RISK PARAMS / TRADES(/mo) / M-PNL / PF / MAXDD / WORST-D / W-VAR / WF / ACC / +(命名規則 MMDD MODEL #N 自動)。
- **Performance**:WIN RATE 拆成 LONG / SHORT 兩卡。
- **terminal 修補**:FACTOR # 入允許名單、factor strategy 不再被降級成 trend。
- 待辦(下一批,codex 正在改同區):param UI 深度重組(MODEL SETTING / ENTRY TRIGGER / 精簡 RISK、移除 risk 區殘留 SL/TP anchor)、sweep 按鈕 run/lock 方塊。
- 驗證:py_compile 全部 + node --check + pytest 23。cache-bust `&winlock1=1`。後端需重啟。

### 1.0.9 批次H:sweep preset 快照(重現性修復)+ 命名唯一性 + 0708 修復

- **根因(0708 preset 回測對不上 sweep)**:`saveSweepPreset` 用「當前表單」`collectStrategyParams('bt')` 當 base,再蓋 `r.params`(只含被掃維度)→ 表單的 session/size/trail/exit/stop 全部漏進 preset(0708 實例:size=3 混入、DAY ZONE preset 夾帶 factor 參數)。回測自然與榜單完全不符。
- **修法**:`sweep._run_one` 對每列輸出 **`preset_params` 完整參數快照**(`_PRESET_SNAPSHOT_KEYS` 60+ 鍵,含 factor_session_va_filter);前端 `+` 一律用快照、不碰表單(舊檔無快照 → 回退舊法 + 警告)。現有 `data/sweep_results.json` 以 dummy-engine 重放參數組裝**逐列回填**(389/389 label+model 驗證匹配)。
- **命名唯一性**:`buildPresetName` 加 while-guard —— 同名(同日同模型同 token)已存在則 #N 遞增,杜絕重存靜默覆蓋(「命名重複」假象)。
- **0708 FACTOR #2 已修復**:params 換成榜單快照(sess ALL/stop1/trail off,保留使用者 3 手 — 只縮放 $ 不改信號),重命名去除重複 token;**引擎驗證重現**:@x1 → 22 筆 / PF 4.37 vs 榜單 21 筆 / PF 4.30(差=新資料幾小時)。`0708 DAY ZONE #1 手動測試` 為 fade+factor 混合殘骸,保留未動(等使用者決定刪否)。
- **注意**:該列 T21 樣本極小,PF 4.30 屬過擬合高風險組;重現性修好 ≠ 該策略可信。
- 待辦:param UI 深層級重構(#6)regenerate 後續批次(codex 同區編輯中,規格已鎖)。
- 驗證:node --check + py_compile + 快照回填 389/389 + 逐位重現。cache-bust `&snap1=1`。

### 1.0.9 批次I:param UI 層級重構(#6)— MODEL SETTINGS 合併 + RISK 精簡

- **合併**:刪除 ORDER & EXIT 標題 → TP ANCHOR(exit mode)/ENTRY TRIGGER/ENTRY(market|limit 唯讀)/TP INPUT(RR)/LADDER/TRAIL 併入 **MODEL SETTINGS**;TIMING 更名 **ENTRY TRIGGER**。
- **歸位**:fade 進場模式 + factor 全區塊(FACTOR FAMILY/SIDE/SIGNAL MODE/VA FILTER/**SL·TP ANCHOR+INPUT**/DAILY MAX)由 RISK 標題下方移入 MODEL SETTINGS —— SL/TP anchor 不再「殘留在 risk management」。
- **RISK 精簡為封鎖型**:FULL LOSS LOCK / FULL WIN LOCK / HIGH VOLATILITY LOCK / SESSION MAX TRADE LIMIT / MARKET LIMIT。**HOLD BARS 隱藏**(hidden input 保留 id/值,preset 套用不受影響);MAX / DAY 更名 **DAILY MAX TRADE LIMIT**。
- **事故與修復**:首刀的鎖塊搬移 regex 未綁 id 後綴 → bt 鎖塊被二次切走插進 live 面板、live fade/factor 區塊連鎖位移進 bt 面板。以「塊內必含 tr-daily-stop-{suf}」+ rfind 位置法重排,最終序驗證:bt(fade367/factor375 → RISK472 → session504)、live(fade819/factor827 → RISK924 → session956),44 個關鍵 id 全唯一。
- 待辦:sweep run/lock 方塊面板(規格保留)。驗證:node --check + pytest 23。cache-bust `&regroup1=1`。

### 1.0.9 批次J:sweep run/lock 面板 — #6 重構完結

- **UI**:SWEEP 按鈕右側新增 run/lock 方塊(`#sweep-runlock`):TREND / DAY ZONE / DISTRIBUTION / FACTOR 四個勾選(預設全勾)+ 鎖定徽章 **LOCK: MNQx1 + RISK**(合約/手數/風控不參與掃描變體)。按鈕更名 SWEEP(還原文案同步)。
- **鏈路**:`runBacktestSweep` 收集勾選 → `body.sweep_models`(全勾不帶=跑全部;零勾攔截);`BacktestRequest.sweep_models`;`run_model_sweep(..., models=None)` 以 `_on(name)` 過濾四段(進度 offset 不變,總數仍顯示全 grid)。
- **驗證**:dummy-engine 實測過濾 — 全跑 389 / FACTOR-only 44 / TREND+DAY ZONE 201,model 集合正確;py_compile + node --check + pytest 23;runlock ids 唯一。cache-bust `&runlock1=1`。後端需重啟。
- **#6 規格至此全部落地**:MODEL SETTINGS 合併(含 ENTRY TRIGGER / ENTRY 顯示 / SL·TP anchors 歸位)、RISK 精簡五項、HOLD BARS 隱藏、DAILY MAX TRADE LIMIT、FULL WIN LOCK、run/lock 面板。

### 待做(P0 後續)
- 觀察每日 match_rate 是否穩定 ≥0.9(7/3 = 0.85,差在 1 分鐘時序造成的 3 筆);可評估是否讓回測也採「上一根已完成 K」決策以完全對齊時序。
- 影子重放 FAIL 日的 live-only 逐筆歸因(重啟/state/lock/多帳號)→ 修 → 看連續通過天數爬升。
- P4 idea 工廠(ORB/VWAP/RSI-2/PDH-PDL sweep-reclaim…)逐週消化;P5 MBO/GEX 數據升級待 P0-P4 結論。
