# 1.0.8 FIX LIST — 結構清理 / 冗餘移除

> 目的：清理雙 AI 開發遺留的冗餘、死碼、命名不一致、亂碼；**不改動核心交易演算法行為**。
> 每完成一項在「進度日誌」記錄 `file:line — 做了什麼`。
> 基準 commit: `d0a9b85 1.0.8a fees commision align`（開工前 git 乾淨）。

---

## A. 安全項（與「留哪些策略」的決定無關，直接做）

| # | 項目 | 檔案 | 狀態 |
|---|------|------|------|
| A1 | 修 2 行亂碼 `????` | ancserTPX.js:1946,1967 | ✅ |
| A2 | `_round_to_tick` 加註解（**不改邏輯**，符合 TopstepX 下單規範保留） | engine.py:2643 | ✅ |
| A3 | 刪死類 `ConsolidationDetector`（~397 行，零呼叫者）+ 修檔頭引用不存在的 reversion.py | consolidation.py | ✅ |
| A4 | 刪死包裝 `_sl_from_largest_tf`（零呼叫者；largest/smallest TF 選擇邏輯走 `_sl_from_reference_tf`，不受影響） | confluence.py | ✅ |
| A5 | `VA_BAND_PCTS` 去重（volume_profile 為單一來源，confluence import） | confluence.py / volume_profile.py | ✅ |
| A6 | 移除死 import（confluence_worker.py `Optional`；consolidation.py `uuid`/`VolumeProfileResult`）。註：engine.py `datetime` 經查**實際有用**，未動 | 多檔 | ✅ |
| A7 | 刪死函式 `_run_single_combo`（113 行，零呼叫者） | routes.py | ✅ |
| A8 | 加 `.editorconfig` 強制 utf-8 + 統一換行，防止亂碼復發 | .editorconfig | ✅ |
| A9 | 刪除 8 個舊版一次性研究/throwaway scripts（git 可還原） | scripts/ | ✅ |

## B. 待「策略決定」後才做（可能砍 confluence 或只砍 mlc2）

| # | 項目 | 檔案 | 狀態 |
|---|------|------|------|
| B1 | 徹底刪除 ml_consolidation_v2 (mlc2) | routes/engine/models/mlc2_live/terminal_live/前端 | ✅ |
| B2 | 徹底刪除 ML sweep + conf-combo 死端點與機制（~600 行） | routes.py | ✅ |
| B3 | 三份 exit/fill/pnl loop → 只留 trend（其他刪/合併） | backtest/*.py | ✅ |
| B4 | one_trade_per_session 變體統一改名 `one_trade_per_session` | 多檔 | ⏭ 跳過（`tr_`/`conf_` 為獨立語意旗標,盲改會併語意+破壞 preset JSON;`one_trade_per_session_direction` StrategyParams 欄位為死 fallback。屬 C 風險級,暫不動） |
| B5 | 刪 LEARN result 板塊 | 前端 + routes | ✅ |
| B6 | `_normalize_value_area_pct` 雙定義收斂（依「鎖 80」需求保留 85 版） | routes.py:85,204 | ✅ |

## C. 核心相鄰、需逐項驗證（低風險但碰 PnL/lock）

| # | 項目 | 檔案 | 狀態 |
|---|------|------|------|
| C1 | trade-date（CT 17:00 / ASIA 開盤為新日）收斂成單一 helper | engine.py + backtest | ☐ |
| C2 | breakout/session-direction lock 狀態三處收斂（避免 desync） | engine.py | ☐ |
| C3 | 統一命名 `_open_position` / `_pending_order`（僅改名，不併語意） | 多檔 | ☐ |

## D. 較大重構（列出，暫不動）

- routes.py：Trade→Response 映射 ×4、Metrics 建構 ×4、equity 迴圈 ×3 抽 helper
- 前端：backtest/live 兩塊 270 行重複面板模板化；重複 helper (fmtTime/fmtMoney/_set) 合併
- CSS：刪未用 class、未定義變數、重複 scrollbar 規則

---

## 進度日誌

（每完成一項追加一行：`[時間] #編號 file:line — 做了什麼`）

### A 批次（安全項）— 全部完成，`pytest` 23 passed，backend compileall OK

- A1 `frontend/static/ancserTPX.js:1946,1967` — 兩處 `????...` 亂碼改為 `'交易引擎啟動成功 ✓ 開始運行'`。
- A2 `backend/live/engine.py:2643-2650` — `_round_to_tick` 加中文註解說明刻意保留全域 TICK_SIZE 符合 TopstepX 規範；邏輯未動。
- A3 `backend/strategy/consolidation.py` — 刪除 `class ConsolidationDetector`（原 71-466，397 行，全庫零呼叫者）；檔頭改寫（移除不存在的 reversion.py、更新為現行三偵測器）；保留 `ZoneEvent`（其他偵測器共用）。
- A4 `backend/strategy/confluence.py:415-421` — 刪死包裝 `_sl_from_largest_tf`（零呼叫者）；largest/smallest TF 錨定仍由 `_sl_from_reference_tf` + `conf_sl_reference_tf` 提供。
- A5 `backend/strategy/volume_profile.py` 新增模組級 `VA_BAND_PCTS` 單一來源；class attr 改為引用；`backend/strategy/confluence.py` 改 import、刪本地重複定義。驗證三處值一致。
- A6 `backend/backtest/confluence_worker.py:31` 移除未用 `Optional`；`consolidation.py` 移除因 A3 而未用的 `uuid`、`VolumeProfileResult`。engine.py `datetime` 查證仍在用故保留。
- A7 `backend/api/routes.py` — 刪死函式 `_run_single_combo`（113 行，零呼叫者）；保留其後 ML/CONF_COMBO 常數（屬 B2 待決）。
- A8 新增 `.editorconfig`（charset=utf-8, py/js/css/html eol=lf, bat/ps1 eol=crlf）防止亂碼復發。
- A9 `git rm` 8 檔：audit_live_vs_backtest_20260618_22、confluence_market_staged_sweep_20260624、cooldown_exit_study_20260623、pending_timeout_study_20260624、session_lock_risk_study_20260623、confluence_exit_check、confluence_market_order_test、confluence_market_rr_sweep。（ML 相關 sweep 腳本待 B 決定後處理）

### B 批次（策略決定：留 trend + confluence，徹底刪 mlc2/ml_trend）— 完成，`pytest` 23 passed，backend compileall OK

- B6 `backend/api/routes.py:85,204` — `_normalize_value_area_pct` 雙定義收斂：刪除較晚定義的 clamp 版（原 line 204，會遮蔽前者導致「鎖 80」失效），保留 `return 0.80` 版（line 85）。修復 lock-80 靜默失效 bug。
- B1 `backend/api/routes.py` — 移除所有 mlc2 / ml_trend 相關端點與程式碼；保留 trend-overlap/confluence/detect-zones 共用 helper（`_get_merged_zone_timeline` 鏈、`_request_payload`、`_weekly_stats`、`ML_TIMEFRAMES`）。留 `# 1.0.8: 移除 ...` 標記。
- B2 `backend/api/routes.py` — 刪除 ML sweep + conf-combo 死端點與機制（含 `_run_ml_consolidation_v2_backtest` 等），累計移除 ~1100+ 行。
- B1 `backend/db/models.py` — 移除 `StrategyParams` 的 mlc2_* 欄位（原 382-395），改註解標記。
- B1 `backend/terminal_live.py` — 移除 mlc2_* 預設（原 89-99）、normalized_strategy 的 mlc2 分支（line 316 收斂為 `confluence` 否則 `trend`）、mlc2 allowed_keys。
- B1/B3 `backend/live/engine.py` — strategy_mode 收斂為「非 confluence 即 trend」；移除 mlc2 evaluator 初始化/get_status 欄位/warmup/兩處 `mlc2_evaluator.update`/dispatch/`_evaluate_ml_consolidation_v2`/`get_ml_consolidation_v2_signals`/`_market_risk_limit_ticks` mlc2 分支；`_pending_mlc2_payload` 相關全刪。**保留** `_place_market_entry`（共用，callers 2448/2526）與 exit/fill/pnl loop 的 trend 路徑。核心行為未改。
- B1 `git rm` 三檔：`backend/strategy/ml_trend.py`、`backend/backtest/ml_trend_backtest.py`、`backend/live/ml_consolidation_v2_live.py`。
- B1/B5 `frontend/static/ancserTPX.html` — 移除兩處 `<option value="ml_consolidation_v2">`（strategy-bt/strategy-live）、LEARN RESULT tab 與整個 `#btab-learn` 面板。
- B1/B5 `frontend/static/ancserTPX.js` — 移除 mlc2 預設 params、`normalizeStrategyName` mlc2 映射、`updateStrategyParamVisibility` isMLC2、`buildStrategyParams` mlc2 區塊、preset 標籤/purpose、tab 切換 `'learn'` + `loadLearnResult()` 呼叫、live start mlc2 記錄、status render `isMlc2`/`mlc2_signals`/`mlc2_shadow`/`|| st.mlc2_mode`、`loadLearnResult` + `_renderScorerCard` 函式。均留 `// 1.0.8:` 標記。
- B4 保留待決：`one_trade_per_session` 變體改名 gate 於策略層命名決定，暫緩。

### Gate 對齊（live == backtest）— 完成，`pytest` 23 passed，backend compileall OK

- 背景：live 的 trend 策略在 evaluate 前多了一道「所有 TF 同方向突破」gate（`_strategy_breakout_observable`），backtest 沒有。A/B 回測（preset #2/#3/#4）證實此 gate 對 overlap preset #3 幾乎毀掉績效（+6051→+502 PnL），#4 總 PnL 略降、風險調整指標略升，#2（單 TF）無影響。
- `backend/live/engine.py` — 移除 evaluate 前的 all-TF gate；warmup observe（原 :1855）與 realtime observe（原 :3494）改為只判 `_trend_session_allowed`；刪除 `_all_tf_breakout_required` / `_strategy_breakout_observable` 方法定義（留 `# 1.0.8:` 標記）。**保留** `_tf_breakout` / `_update_tf_breakout` / `_tf_breakout_summary`（僅供 UI PHASE 顯示，不再 gate 交易）。
- `backend/backtest/engine.py` — 移除研究期暫加的 `require_all_tf_breakout` 旗標與相關 scaffolding（constructor 參數、init、4 個移植方法、gate block、`_tf_breakout` reset）。回測回到原本無 gate 行為。
- `git rm scripts/gate_ab_compare.py`（一次性 A/B 研究腳本）。
- 結果：live 突破判定與 backtest 一致，皆由 `trend_follow.evaluate` 對交易 zone 判斷。

### Preset 改版：移除 CODEX #1、新增 CLAUDE #1/#2/#3、開放 VA 70/80 — 完成，`pytest` 23 passed，backend compileall OK

- 背景：VA70/80 x RR 全掃（`scripts/va_rr_study.py`，4 preset x 2 VA x 5 RR）排行榜。使用者選前段 rank #2/#4/#5 成為 CLAUDE 系列。
  - CLAUDE #1 = rank #2：單5m **VA70** RR4 C3 SL80 Trail50L10 SesON（+7218 PF1.47 Calmar9.1 win36%，DD 最低）。
  - CLAUDE #2 = rank #4：重合30m1h小TF **VA70** RR6 C4 SL80 SesON（+6134 PF2.06 Calmar10.0）。
  - CLAUDE #3 = rank #5：重合5m30m小TF **VA80** RR6 C3 SL80 SesOFF FT2（+6087 PF1.94 Calmar10.6，= 原 CODEX #3 設定）。
- `backend/api/routes.py` + `backend/terminal_live.py` 兩套 registry 同步：
  - `_codex_626_trend_preset` / `_trend_preset` 新增 `value_area_pct` 參數（預設 0.80）。
  - 新增 `CLAUDE_701_PRESET_1/2/3` 名稱常數 + `_BUILTIN_PRESETS`/`BUILTIN_PRESETS` 條目。
  - 移除 CODEX #1（`_CODEX_630_PRESET_1` 加入 `_REMOVED_PRESET_NAMES`；terminal_live 自 BUILTIN_PRESETS 拿掉）。
  - `DEFAULT_LAST_USED_PRESET` 由 CODEX #1 改指 CLAUDE #1。
- **解除 v1.0.6 的 VA 硬鎖 80**：`_normalize_value_area_pct`（兩檔）由 `return 0.80` 改為吸附 {0.70, 0.80} 最近檔、無法解析回 80；normalize 迴圈的 `value_area_pct = 0.80` 強制行改走 normalizer，保留 preset 的 70。→ 只有明確設 70 的 preset 會變，其餘一律 80，不影響既有。
- 註：CODEX #4 名義 RR7 仍被 `_build_strategy_params` 的 `min(6,…)` 夾成 6（既有行為，未動）。

### 功能：TIMEFRAMES 新增 SESSION 選項（0.15.5 式整段 session 生長區間）— 完成，`pytest` 23 passed，session-TF 端到端 smoke OK

- `backend/strategy/consolidation.py` — `SessionZoneDetector` 補 `get_recent_zones()`（回傳當前生長中 zone，對齊 ClockBucket/Overlap 介面）；`_create_zone` 標 `timeframe="session"`；`build_zone_detector` 加 session 分支（優先於 overlap，忽略 tf_combo）。
- `backend/api/routes.py` — `AREA_TIMEFRAME_CHOICES` + "session"（`_normalize_area_timeframe` 即接受）；detect-zones `all_timeframes` 改跑 `ML_TIMEFRAMES + ("session",)` 讓圖表能畫 session zone。
- `backend/terminal_live.py` — `_build_strategy_params` 放行 area_timeframe="session"，並強制 method=single / tf_combo=[]。
- `backend/live/engine.py` — zone 快照 restore 的 `_completed_zones` hasattr guard 訊息更新（session/overlap 皆走 warm-up 重建，不讀快照）。
- `frontend/static/ancserTPX.html` — bt/live 兩組 TIMEFRAMES 各加 SESSION checkbox；JS cache-bust `&sesstf=1`。
- `frontend/static/ancserTPX.js` — `enforceSessionTfExclusive()`：勾 SESSION → 其他 TF 取消+鎖灰；取消 → 解鎖、無選則回落 5m。掛在 `onTfSelectionChange`（點擊路徑）與 `setOverlapTfCombo`（preset 載入路徑）；`readOverlapTfCombo` order + 'session'。
- 需重啟 web server 生效（無 --reload）。

### FABLE 雙引擎（ladder 出場 + 日虧斷路器 + FADE 前日VA回歸策略 + 07.02 FABLE presets）— 完成，`pytest` 23 passed，全端到端驗證 OK

- 背景：一週深度研究收斂 — 進場端所有變體（量價過濾/大結構/確認數/掃損反打/POC深掛/時段）全部無 edge；唯二可用槓桿 = 出場工程 + 日級開關。組合回測（2.5月）：ladder+stop4 ⊕ fade×2 = **+7549 / DD 489 / 最差日 -247**（vs 原單書 +7181/DD791/-675）。
- **新參數**（`backend/db/models.py` StrategyParams）：`tr_exit_mode`（"tp" 現行 | "ladder" 無TP階梯滾動：+2R 時 SL→entry，之後每 +1R 跟 1R，恆落後峰值 2R）；`tr_daily_loss_stop`（當日虧損單數達 N 停新單，0=OFF）。
- **新策略**（`backend/strategy/fade.py` `PrevDayFade`）：買 limit @ 前日 VAL → TP 前日 POC，SL 80t（回測證實不可收窄），只做多（short 腿弱已砍），每日最多 1 次（策略自管鎖：發信號上鎖/掛單過期解鎖/成交鎖到日終），距 POC <8t 不做，趨勢日（價格在前日 VA 外）不接。
- `backend/backtest/engine.py`：strategy_mode +fade（trend_follow 槽位換 PrevDayFade，管線不變）；`_process_candle` 頂部交易日 rollover（斷路器計數重置 + fade 前日 VP 餵水位）；signal 後斷路器閘；`_execute_entry` ladder 記 R + 推遠 TP；`_check_trailing_sl` ladder 分支 → 新 `_check_ladder_sl`（多段棘輪，收盤價計）；`_execute_exit` 虧損計數。
- `backend/live/engine.py`：strategy_mode +fade；`_refresh_fade_levels()`（warm-up 完成 + CT17:00 rollover 時以前日全部 1m 算 VP，<60 根不掛單）；`_place_order` ladder 模式 TP bracket 推遠 2000t；`_check_trailing_sl_live` 頂部分派 → 新 `_check_ladder_sl_live`（多段 modify_order 改 Auto-OCO SL，失敗下一 tick 重試，浮盈以 `_last_market_price` 計 — 與既有 live trail 同慣例，較回測的收盤價略敏感）；斷路器（出場虧損計數/進場閘/start+rollover 重置）；fill/flatten 清 ladder 狀態。
- **Presets**（兩 registry 同步）：`07.02 FABLE #1 動量書 均衡`（ladder+Stop4）/`#2 進攻`（ladder+StopOFF）/`#3 最小最差日`（ladder+Stop3）+ `FABLE Fade前日VAL接多 MNQx2 / MNQx1`。預設 preset 改指 FABLE #1。組合對照：#1⊕fade×2=+7549/DD489；#2⊕fade×2=+10773/DD998；#3⊕fade×1=+5525/DD498（最差日-165）。動量書與 Fade 書須跑在兩個帳號。
- 放行/傳遞鏈：`_normalize_strategy_name`+fade（routes/JS/兩處存檔 normalize）；BacktestRequest/LiveStartRequest + 兩欄位；`_build_strategy_params`/`_build_strategy_params_from_request` 傳遞；allowed_keys；`MNQ_SIZE_CHOICES` +2（Fade x2 用）。
- **順手修 BUG**：`terminal_live._build_strategy_params` 從未把 `value_area_pct` 放進 StrategyParams → 引擎永遠用預設 0.80，VA70 preset 在 terminal 直啟路徑實際跑 80%（web 回測路徑不受影響）。已補傳。
- 前端：STRATEGY 下拉 +「FADE 前日VA回歸」；RISK 區 +EXIT MODE（TP固定/LADDER階梯滾動）與日虧斷路器（0-6）兩控件（bt/live）；buildStrategyParams/applyStrategyParams 讀寫；cache-bust `&fable=1`。
- 驗證：引擎原生結果與研究腳本逐位一致（ladder+stop4 260筆/+5039.6/DD498.3；ladder 541筆/+8263.7/915.4；fade 31筆/+1254.6/247.4）；routes 請求路徑 e2e（FABLE#1 → exit=ladder stop=4 VA=0.70；FADEx2 → fade/size2/全時段）；node --check、compileall、pytest 23 passed。研究腳本（scripts/*_study.py 等 14 個）依使用者要求保留。
- ⚠️ 需重啟 web server；fade live 需 warm-up ≥ 前一完整交易日（現行 warm-up 抓 2-14 天，足夠）。

### 圖表修復 + UI 開關 + 合約自動換月 + 高效回測 — 完成，`pytest` 23 passed，node --check OK

- **圖表 zoom 寬度修復**（`ancserTPX.js` drawPositionTools）：根因 = `timeToCoordinate` 對可視範圍外時間回 null 時退回固定像素寬（`entryX+120px`）→ 縮放時 2 分鐘的單被畫成長條。新 `timeToX()`：null 時用可視區「秒/像素」線性外推，交易框/決策 zone 一律按真實時間畫。RR 框依使用者要求**移除 entry/TP/SL 三條線只留紅綠底**；決策 zone（primary_zone）保留白色 VAH/VAL 線並以 formed_at→left_at 真實時距畫（4h zone = 4h 寬）。
- **EXIT MODE 灰化**（`onExitModeChange`）：選 LADDER → RR RATIO / TRAIL TP TRIGGER / TRAIL SL 鎖灰（透明度 0.35 + tooltip 說明階梯常數固定）；切回 TP 還原。掛在切換與 preset 載入兩路徑。
- **FADE 的 TF 鎖**（`enforceFadeTfLock`）：策略選 FADE → TIMEFRAMES 顯示鎖定的 DAY chip（前一整個交易日），其他 TF 取消勾選並鎖灰;離開 fade 還原（無選擇回落 5m）。
- **合約自動換月**：`backend/db/models.py` 新增 `current_quarterly_contract_id()`（CME 股指 H/M/U/Z，到期=季月第 3 個週五，前 8 天換月；驗證：now→U26、9/11→Z26、12/15→H27）與 `normalize_contract_id_to_front()`。兩 registry 的內建 preset 改為開機計算；存檔 preset normalize 迴圈自動改寫舊到期合約（auto-renew）；routes `_build_strategy_params_from_request` 對請求合約一律轉前月；JS 鏡像 `currentQuarterlyContractId()` + `refreshContractOptions()` 開機重寫所有選項/輸入值 + `DEFAULT_STRATEGY_PARAMS`。
- **高效多變體回測**（審計結論）：`BacktestEngine` 早有 `zone_timeline` 快路徑（ML grid 用）但全庫無 trend builder。`ClockBucketZoneDetector.get_recent_zones()` 只回「已完成 bucket」且完成即凍結 → 逐 K 快照可共享引用零拷貝。`scripts/session_ladder_sweep.py` 實作 `build_trend_zone_timeline()`：detector 跑 1 次，24 個參數變體共用（估 ~60 分 → ~20 分）。（builder 目前研究腳本層級;若要進核心可後續移入 backend/backtest/。）
- cache-bust `&chartfix=1`。

### FABLE 改名 + UI 重排 + 交易考古（快照庫）+ 單TF回測 75x 加速 — 完成，`pytest` 23 passed，timeline 平價驗證逐位一致

- **Preset 改名**：動量書→Trend、Fade 編號 #4/#5（`07.02 FABLE #1-3 Trend…` / `#4 Fade MNQx2` / `#5 Fade MNQx1`）。舊名加入兩 registry 的 `PRESET_RENAMES` — 使用者存檔自動遷移。
- **UI 重排**：EXIT MODE 移到 RISK MANAGEMENT 第一項（獨立 form-group）；日虧斷路器獨立一行；移除先前兩者同列的 form-row（bt/live 兩面板）。
- **實盤策略考古**：`data/trades.json` 記錄新增 `exit_mode` / `daily_loss_stop` / `param_snapshot_id`（`strategy` 欄位本就記 strategy_mode，fade 會標 "fade"）。新 `_register_param_snapshot()`：引擎啟動時把完整 StrategyParams+模式+合約+手數 canonical-JSON→sha1 前 12 碼，**append-only 寫入 `data/strategy_snapshots.jsonl`**（同配置去重）。preset 刪了/改名，每筆單當時的完整參數永遠可由 snapshot_id 反查。
- **單 TF 回測 75x 加速**（routes /backtest trend 路徑）：發現 `_precompute_zone_timeline` + `_get_precomputed_zone_timeline`（快取）早已存在（ML 基建）但單 TF 回測沒接。現接上：非 overlap 且非 session 的 trend/fade 回測 → 快取 zone timeline（同 TF+VA+數據簽名命中即秒回）。timeline 模式引擎跳過內排 → 供給已排序 candles。**平價驗證：timeline vs detector 路徑 260 筆 / +5039.6 / DD498.3 / PF1.84 逐位一致**；引擎迴圈 1.5s vs 35s，首建 46s 後續快取。session 生長 zone 會變異不適用快照，維持原路徑。
- 研究側 `scripts/session_ladder_sweep.py`（獨立 builder）結論：**ASIA 仍壓倒性最佳**（ladder +8462 vs ALL +7568 且 DD 減半）；RTH -2316 絞肉機；AH 被 flatten 窗鎖死；C3 仍是 confirm 均衡點。FABLE 配置不需調整。

### 圖表錯位 HOTFIX + Metrics 卡片重排 — 完成，div 185/185 平衡，node --check OK

- **HOTFIX 圖表錯位**：前一批 UI 重排的刪除 regex（`</div>\s*</div>\s*</div>`）比實際區塊多要求一個閉合 → 兩面板各被吃掉「TRAIL TP TRIGGER / TRAIL SL / SESSION LIMIT 列 / tr_allowed_sessions + `tr-params` 容器閉合」→ DOM 失衡（+2 open div）→ chart 被擠到左下。已從 git HEAD 抽取原始片段回植（live 段裁去多餘 panel 閉合），div 185/185 平衡、8 個控件 id 全數恢復。教訓：對 HTML 用結構化錨點，不用貪婪跨層 regex。
- **Metrics 卡片重排**（renderMetrics）：TOTAL LOSS 後新增 **WORST DAY**（當前窗口最差單日,含實盤括號對照）；**WIN RATE 全寬**移至 EXIT % 之前；**CURRENT ZONE TP/SL/TRAIL 全寬獨立一行** → ASIA/EURO/PRE/RTH 兩兩自動對齊。卡片模板支持 `full: true`（grid-column: 1 / -1）。cache-bust `&uifix=2`。

### SWEEP 進後端 + SWEEP 分頁 + MONTHLY GAIN AVG 卡 — 完成，`pytest` 23 passed，144 變體煙測 OK

- **`backend/backtest/sweep.py`（新模組,0.15.0 sweep 回歸版）**：`build_trend_zone_timeline()`（timeline 快照 builder 正式進核心）+ `run_trend_sweep()`（grid：VA70/80 × [TP RR2-6 + ladder] × C2-5 × 斷路器0/3/4 = 144 變體;每變體回報 trades/win%/pnl/**gain/loss/PF**/maxDD/**monthly_avg/worst_day**/expect/score）。score = pnl/maxDD;gain/loss/PF 欄位回應「maxDD 低但 total loss 太大不平衡」的觀察。
- **routes**：`POST /backtest/sweep`（併發 guard、複用 `_update_bt_progress` 進度、結果持久化 `data/sweep_results.json`）+ `GET /backtest/sweep/results`（啟動自動載入）。
- **前端**：底部分頁列新增 **SWEEP**（BACKTEST TRADES 左邊）；EXECUTE BACKTEST 下新增 **SWEEP 144** 按鈕（連線後啟用,跑完自動切到 SWEEP 分頁）；`renderSweepTable()` 可點欄位標題換排序（score/pnl/pf/maxDD/monthly_avg/worst_day…),PF<1.4 標紅提示 gain/loss 失衡;`loadSweepResults()` 開機自動展示上次結果。
- **Metrics**：TOTAL LOSS 後、WORST DAY 前新增 **MONTHLY GAIN AVG** 卡（含實盤括號對照）,與 WORST DAY 同列成對。
- **語義修正（使用者指正）**：MONTHLY GAIN AVG = **30.44 天歸一化月率 run-rate**（`total_pnl × 30.44 / 日曆天跨度`）,前端卡片與 sweep 模組同步。原「日曆月分組平均」被部分月（4 月僅 9 交易日、7 月 2 天）嚴重拖低。驗證：FABLE#1 pnl 5039.6 / 74 天 → $2073/月 ✓（= $5k/2.4月 ≈ $2.1k 直覺）。
- cache-bust `&sweep=2`。

### 待做：C 批次（C1 trade-date 單一 helper、C2 lock 狀態收斂、C3 命名統一）— 觸及核心 PnL/lock，需逐項確認後才動。
