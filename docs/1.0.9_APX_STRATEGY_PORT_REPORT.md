# 1.0.9 APX Strategy Port Report

Generated: 2026-07-03

## Source Repo

Local repo inspected:

- `F:/ancserQuant/ancserAPX`
- `F:/ancserQuant/ancserAlpacaLab`

The useful strategy layer is not a futures scalper. It is a stock factor engine:

- composite factor score
- medium momentum + short pullback
- rank acceleration
- volume/sector acceleration
- drift-regime filter
- volatility targeting
- strict backtest/live parity through shared pure functions

The code was not copied blindly into live TPX. It was translated into a TPX research-only intraday factor script.

## Ported Script

`scripts/apx_intraday_factor_port.py`

The script converts APX cross-sectional stock logic into single-instrument MNQ 5m time-series factors:

| APX concept | TPX translation |
|---|---|
| Momentum 12-1 | 5m medium momentum excluding recent bars |
| Pullback 5d | short 5m pullback inside medium trend |
| Rank Acceleration | time-series momentum percentile acceleration |
| Sector Rank | volume acceleration proxy |
| Drift-Reversion | positive-bar ratio drift filter |
| Vol Targeting | ATR-scaled TP/SL |

Signal rule:

- evaluate completed 5m candle
- enter next 5m open as market order
- exit by ATR-scaled TP/SL or max hold bars

## Result

Output:

- `data/machinelearning/apx_intraday_factor/report.md`
- `data/machinelearning/apx_intraday_factor/results.csv`
- `data/machinelearning/apx_intraday_factor/top_latest.csv`

5m bars:

- 14,778

Variants tested with trades:

- 4,422

PASS:

- 0

Best result:

| Preset | Side | Session | Trades | PNL | DD | PF | Win | Total Loss | Verdict |
|---|---|---|---:|---:|---:|---:|---:|---:|---|
| rankaccel | short | RTH | 51 | 2319.54 | 380.14 | 1.9212 | 58.82% | -2517.94 | FAIL |

Failure reasons:

- first walk-forward third is negative
- total loss is greater than PNL

Cleaner but small-sample result:

| Preset | Side | Session | Trades | PNL | DD | PF | Win | Total Loss | Verdict |
|---|---|---|---:|---:|---:|---:|---:|---:|---|
| rankaccel | short | ALL | 31 | 1643.57 | 397.46 | 2.5558 | 61.29% | -1056.40 | FAIL |

Failure reason:

- sample < 40

## Interpretation

APX rank acceleration is the first imported idea that shows real signal quality above RSI/KDJ and basic hunter/sweep:

- PF near 1.9 on the best sample
- DD under 500 on the best RTH short variant
- positive bootstrap on the top RTH result

But it is still not deployable:

- PNL is too small versus the 6k target
- total loss is still larger than PNL
- one chronological third is negative
- the best clean variants have too few trades

## Practical Decision

Do not add this as a live preset yet.

Keep it as a research candidate:

- name: `APX RankAccel Short`
- likely useful as a filter or veto on RTH long entries
- not enough as a standalone strategy

Next useful test:

- combine `APX RankAccel Short` as a regime filter with Sigma 60m L3
- reject Sigma long entries when rank acceleration is strongly bearish
- allow Sigma short entries only when rank acceleration is bearish

