# ancserTPX runtime dependencies.
#
# Pinned to the exact set actually running (pip freeze 2026-08-08): trading
# behaviour is sensitive to pandas/numpy changes, so CI must not silently
# pick up a new minor version.
#
# ASCII ONLY. pip reads requirements files with the *locale* encoding, not
# UTF-8. A GitHub runner with LANG unset resolves to ASCII, so a non-ASCII
# comment here makes `pip install -r` die with UnicodeDecodeError before a
# single test runs. (This is what broke the first CI run on 6e1b915.)
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
httpx==0.28.1
requests==2.34.2
aiohttp==3.14.3
websockets==14.1
signalrcore==0.9.5
numpy==2.2.6
pandas==2.2.3
# emapmo_messenger renders the 1280x720 signal chart. Imported lazily but with
# no fallback -- missing it is a hard failure, not a degraded mode.
matplotlib==3.10.8
# Slim Linux containers often ship no system tzdata and zoneinfo then raises
# ZoneInfoNotFoundError. Trade-date boundaries, the PI pre-session filter and
# session gating all depend on zoneinfo -- do not leave it to chance.
tzdata
