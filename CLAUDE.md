# ancserTPX — 給 coding agent 的專案規則

> 這些約束先前只存在單一機器的 agent 記憶目錄裡(`~/.claude/…/memory/`)。
> 那是沒進版控、換台電腦或換工具就消失的單點失效。現在放在 repo 裡。

## 開工前

動任何重構、清理、簡化之前:

0. 讀 [`AGENTS.md`](AGENTS.md) —— 真相排序與交接規則(給非 Claude 的 agent 也一樣)
   與 [`docs/HANDOFF.md`](docs/HANDOFF.md) —— 目前基準與未解問題
1. 讀 [`docs/INVARIANTS.md`](docs/INVARIANTS.md) —— 行為不變量清單。
   每條標了「哪個測試證明它」或 `UNPROTECTED`。**數字會變,以那份文件為準**,
   不要相信這裡寫的統計(這行本身就過期過一次)
2. 跑 `python -m pytest tests/ -q`(約 35 秒)
3. 看一眼要動的區域有沒有 `1.0.x:` 開頭的註解 —— 那些通常在記錄某個歷史 bug

## 絕對不要

- **不要**只因為某段程式碼「看起來多餘」就刪掉 observer、fallback、特例分支、
  快取失效路徑、同步路徑。刪之前要能回答:它為什麼存在、保護哪條不變量、
  替代方案怎麼保住它、哪個測試證明等價。答不出來就留著。
- **不要**為了讓某個新實例正常運作,加一條只鎖定它的選擇器 / `data-` 屬性 /
  分支。同一顆共用元件的第二個實例壞掉,壞的是**共用契約**,不是那個實例缺了
  客製化。1.0.10o 就是這樣壞的:CHART LAYERS 的開關被加了只有它有的
  `data-glass-shrink`、只鎖定 `.chart-layer-pop` 的 `visibility:hidden`,
  三個 bug 全出在這些旁路上,修法是把三條旁路**刪掉**、讓它回去用跟
  PI/語言開關同一份契約。判準:如果你正在寫的選擇器包含某個容器的 id 或
  class,停下來問「為什麼別的實例不需要這條」。
- **不要**在同一次改動裡混合「架構重構」和「策略行為變更」。
- **不要**無差別 `kill python` —— 使用者的 uvicorn 交易伺服器也是 python。
  要用 `Win32_Process` 比對 `CommandLine`。
- **不要**在未確認的情況下改動核心引擎行為。

## 這個環境的硬事實

- **伺服器沒有 `--reload`。** 改了 `backend/**.py` 一定要完整重啟
  (關 cmd 再跑 .bat)才會生效。model JSON 的權重和原始碼對不上,
  幾乎都是因為沒重啟。
- **不要預設就跑長腳本**(抓資料 / 回測 / 訓練)。寫好讓使用者跑。
  除非使用者明講「你不能幫我跑嗎」,那就背景執行。
- **Sweep 記憶體天花板**:6 年資料每個 worker 常駐 1.25GB,14 個 worker 會讓
  16GB 機器 thrashing(看起來像卡住)。
- 完整蠟燭庫(210MB/商品)不進版控,備份在 `F:/ancserQuant/ancserData/`。

## 驗證的標準

今天(2026-08-08)的教訓:一次區塊替換靜默刪掉 `pi_listener.py` 的兩個正規
表示式,`ast.parse` 過、`import` 過、142 個測試全綠,但實盤 listener 會在
收到第一個訊號時 crash。

所以:

- **`ast.parse` / `import` / 既有測試綠燈都不算驗證。** 要驗的是
  「這條路徑被實際執行過」—— 對 parser 就餵真實輸入。
- 用 `index()` + slice 做區塊替換很容易吃掉相鄰程式碼。改完 `git diff --stat`
  對刪除行數。
- 寫測試時要有一條**正向斷言**:「這個輸入本來會產出東西」。
  否則被測函式壞掉回空時,「有被過濾掉」的測試照樣會綠。
- **互動的 bug 必須驅動互動才看得見。** 只讀靜止狀態的 DOM / computed style
  永遠測不到它。1.0.10o 的三個開關 bug 全程通過自己那批只驗 resting state
  的測試 —— 拖曳中途去讀才會看到 `layerOpacity: 1` 但 `layerVisibility:
  hidden`。要用真滑鼠 down/move、在中途取樣,必要時逐幀錄。
- **新測試寫完,把 bug 放回去確認它會紅。** 綠燈只證明「現在是綠的」,沒有
  證明這個測試抓得到那個 bug。這一步抓過「斷言寫錯對象所以永遠綠」。
- 「不該存在」的斷言要對**去掉註解後**的原始碼做。這個 repo 習慣在刪掉某段
  程式碼的地方留一條說明它為什麼被刪的註解,直接對全文 `not in` 會被自己的
  說明打到,逼人刪掉說明(見 `_code()` helper)。
- 量測隱藏面板裡的 DOM 會拿到 0×0;rAF 在背景分頁不會前進。
  兩者都會製造偽陽性,先確認 `document.hidden`。

### 不要用時間賽跑當斷言

CI 的 runner 是共用的,`asyncio.sleep(0.01)` 可能實際睡 60ms。

```python
# 壞:「太快就失敗」—— 1.0.10f 就是這樣紅的
await asyncio.sleep(0.01)
assert not task.done()          # runner 一慢,工作就做完了

# 好:直接驗機制
assert worker_thread_id != loop_thread_id
```

判準:**timeout 當安全閥可以**(`wait_for(x, timeout=5)` —— 只有真的卡住才紅);
**sleep 當斷言不行**(假設某件事「還沒做完」)。

要驗「沒有阻塞 event loop」就比較 thread id;要驗「有依序發生」就用
`threading.Event` 同步。會偶爾紅的測試比沒有測試更糟 —— 它訓練人忽略紅燈。

## 研究紀律

- Sweep 網格**必須先能逐筆重現已知 preset**,對不上就不要看任何掃描結果。
- 每個掃描維度都要確認它**真的會改變結果**(改了數字沒動 = 那個參數沒接上)。
- 看**高原**不是看尖峰。小樣本下任何格點的「最佳」都必然好看。
- 引擎**同時只持有一個部位**。研究腳本若允許重疊持倉,n 會嚴重虛增
  (實測 PI 從 34 筆變 18 筆、績效砍 70%)。
- 任何「當日累計」類的閘門都要問:這個數字在決策當下真的知道嗎?
  (實測前視偏差讓當日停損從 PF 2.87 假裝成 4.15。)

## 版本與提交

- 版本號寫在 commit message(`1.0.10d ...`)。HTML header badge 與
  `?v=X.X.X` cache-bust 只是瀏覽器快取用,不是專案版本。
- 使用者直接推 main(GitHub Desktop),沒有 PR 流程。
- **除非使用者明講,不要 commit。** 版本號和說明由使用者自己寫。
